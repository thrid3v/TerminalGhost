# Runtime dependencies — keep in sync with pyproject.toml [project.dependencies].
# Prefer installing via pyproject: `pipx install terminalghost` (users) or
# `pip install -e ".[dev,cloud]"` (development), which read pyproject.toml directly.
httpx>=0.27
psutil>=6.0
rich>=13.7
textual>=0.60

# Optional extras (pyproject [project.optional-dependencies]):
#   cloud  -> anthropic>=0.30   (only for the Claude backend)
#   dev    -> pytest, pytest-asyncio, pytest-cov, ruff, build
