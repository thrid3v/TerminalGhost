# Runtime dependencies — keep in sync with pyproject.toml [project.dependencies]
httpx>=0.27
anthropic>=0.30
tomli-w>=1.0
rich>=13.0
psutil>=6.0
ptyprocess>=0.7
watchdog>=4.0

# Dev / test dependencies
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
