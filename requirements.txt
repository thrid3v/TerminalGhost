# Runtime dependencies — keep in sync with pyproject.toml [project.dependencies]
# (prefer `pip install -e ".[dev]"`, which reads pyproject.toml directly)
httpx>=0.27
anthropic>=0.30
psutil>=6.0

# Dev / test dependencies — pyproject.toml [project.optional-dependencies] dev
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
