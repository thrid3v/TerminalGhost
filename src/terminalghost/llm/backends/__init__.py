# terminalghost.llm.backends — concrete backend implementations
#
# Submodules:
#   ollama  — Ollama local HTTP API backend (default)
#   cloud   — Cloud API backend (Anthropic Claude + OpenAI stub)
#
# Do not import backends directly from user code;
# use `from terminalghost.llm import get_backend` instead.
